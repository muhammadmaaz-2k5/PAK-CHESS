const COOKIE_MAX_AGE = 24 * 60 * 60 * 1000; // 24 hours
const GAME_TIME_MS = 10 * 60 * 1000; // 10 minutes classical time per player
const ABANDON_TIME_MS = 60 * 1000; // 60 seconds abandonment timer

module.exports = {
  COOKIE_MAX_AGE,
  GAME_TIME_MS,
  ABANDON_TIME_MS,
};
